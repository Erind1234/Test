$(document).ready(function () {
                // Store the initial value of the search box
                var initialSearchBoxValue = $("#searchBox").val();

                // Add an event listener for the "Search by REFCODE" button
                $("#searchByRefcode").click(function () {
                    var refcode = $("#REFCODE").val();
                    if (refcode !== "") {
                        // Clear the first search box value
                        $("#searchBox").val("");
                        
                        // Make an AJAX request or perform your action here to replace the table content
                        // For example, you can use $("#resultTable").html(newTableHTML);
                    }
                });

   $("form").submit(function (event) {
                    event.preventDefault(); // Prevent the form from submitting
                    var searchTerm = $("#searchBox").val();
                    if (searchTerm !== "") {
                        // Make an AJAX request or perform your action here to display the table
                        // For example, you can use $("#resultTable").html(tableHTML);
                    }
                });
            });
