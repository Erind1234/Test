if (string.IsNullOrWhiteSpace(emp.REFCODE) || string.IsNullOrWhiteSpace(emp.PYMTORDNUM) || string.IsNullOrWhiteSpace(emp.PAYERNIPT))
    {
        // At least one of the required fields is empty, add an error message
        ModelState.AddModelError("", "Please fill in all required fields.");
    }



@Html.ValidationSummary(true, "", new { @class = "text-danger" })

<div id="resultTable" class="table-container" style="display: @(ViewData.ModelState.IsValid ? "none" : "block");">
    <!-- Your split table code goes here -->
</div>

