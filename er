$("#searchBy").click(function () {
    var searchBox = $("#searchBox").val();
    if (searchBox !== "") {
        // Set the value of the QRCODEVAL input field
        $("#QRCODEVAL").val(searchBox);

        // Submit the form
        $("form").submit();
    }
});
